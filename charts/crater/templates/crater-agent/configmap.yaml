{{- if .Values.agent.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: crater-agent-config
  namespace: {{ .Release.Namespace }}
  labels:
    app: crater-agent
data:
  platform-runtime.yaml: |
{{ toYaml .Values.agent.platformRuntime | nindent 4 }}
{{- end }}
